---
name: markdown-to-word
description: Convert Markdown (.md) files to consistently formatted Word (.docx) files with the bundled Pandoc reference document. Use for MD-to-Word export; not for editing an existing DOCX.
---

# Markdown to Word

Run `scripts/convert.sh INPUT.md [OUTPUT.docx]`.

Preserve the source content. Render the result to page images, inspect every page, and deliver only after the latest render passes.
